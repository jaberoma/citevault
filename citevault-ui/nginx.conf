server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://citevault-api:8000;
    proxy_buffering off;
    proxy_set_header Connection "";
    proxy_http_version 1.1;
  }

  location / {
    try_files $uri /index.html;
  }
}
